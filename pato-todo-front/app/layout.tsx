import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import OpenLogModalButton from "@/components/openLogModalButton";
import { AuthProvider } from "@/lib/context/authContext";
import { TodoProvider } from "@/lib/context/todoContext";
import OpenCreateTodoButton from "@/components/openCreateTodoButton";
import CreateTodoModal from "@/components/TodoModals/createTodoModal";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <AuthProvider>
      <TodoProvider>
        <html lang="en">
          <body
            className={`${geistSans.variable} ${geistMono.variable} antialiased`}
          >
            <div className="w-full h-12 absolute bg-white flex items-center justify-between px-6 py-2">
              <OpenCreateTodoButton />
              <OpenLogModalButton className="rounded-md border border-black px-3 py-1" />
            </div>
            {children}
            <CreateTodoModal />
          </body>
        </html>
      </TodoProvider>
    </AuthProvider>
  );
}
